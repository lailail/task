# 高并发购票系统

当前仓库处于 `Phase 1`，目标是先建立 Maven 多模块父工程和基础微服务骨架。

当前阶段只包含：

- 父工程依赖管理
- `common-core` 公共模块
- `gateway-service`
- `user-service`
- `ticket-service`
- `seckill-service`
- `order-service`
- `job-service`

业务功能、库存预扣、异步下单、超时关闭等内容将在后续阶段逐步补齐。
